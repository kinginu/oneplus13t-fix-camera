name: "Update Module"

# Refreshes module/odm with the blobs from a ColorOS OTA, then hands the
# release itself over to the "Release Module" workflow.

on:
  workflow_dispatch:
    inputs:
      oos_version:
        description: "Target OOS Version (e.g., CPH2723_16.0.1.303)"
        required: true
        type: string
      version_suffix:
        description: "Optional Suffix (e.g., _v2)"
        required: false
        default: ""
        type: string
      oos_ota_url:
        description: "Source OOS Full OTA URL"
        required: true
        type: string
      cos_version:
        description: "Source COS Version (e.g., PKX110_16.0.1.301)"
        required: true
        type: string
      cos_ota_url:
        description: "Source COS Full OTA URL"
        required: true
        type: string

permissions:
  contents: write

jobs:
  check-tag:
    runs-on: ubuntu-latest
    outputs:
      release_tag: ${{ steps.check.outputs.release_tag }}
      exists: ${{ steps.check.outputs.exists }}

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Check if release tag exists
        id: check
        run: |
          release_tag="${{ inputs.oos_version }}${{ inputs.version_suffix }}"
          echo "release_tag=$release_tag" >> "$GITHUB_OUTPUT"

          if git ls-remote --tags origin | grep -q "refs/tags/${release_tag}$"; then
            echo "Skipping: tag $release_tag already exists."
            echo "exists=true" >> "$GITHUB_OUTPUT"
          else
            echo "Tag $release_tag is new."
            echo "exists=false" >> "$GITHUB_OUTPUT"
          fi

  update-odm:
    needs: check-tag
    if: needs.check-tag.outputs.exists == 'false'
    runs-on: ubuntu-latest
    env:
      RELEASE_TAG: ${{ needs.check-tag.outputs.release_tag }}

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          ref: ${{ github.ref_name }}

      - name: Prepare environment
        run: |
          cleanup_disk() {
            echo "Cleaning up disk space..."
            sudo rm -rf /usr/share/dotnet
            sudo rm -rf /opt/ghc
            sudo rm -rf /usr/local/lib/android
            sudo rm -rf "/usr/local/share/boost"
            sudo rm -rf "$AGENT_TOOLSDIRECTORY"
            sudo systemctl stop docker || true
            sudo rm -rf /var/lib/docker
            sudo rm -rf /usr/share/docker-desktop
          }

          download_otas() {
            echo "Downloading OTA packages..."
            mkdir -p OTA_zip

            aria2c -x16 -s16 -k1M --file-allocation=none -c -d OTA_zip -o "oos.zip" "${{ inputs.oos_ota_url }}" &
            PID_OOS=$!

            aria2c -x16 -s16 -k1M --file-allocation=none -c -d OTA_zip -o "cos.zip" "${{ inputs.cos_ota_url }}" &
            PID_COS=$!

            wait $PID_OOS || { echo "OOS download failed!"; exit 1; }
            wait $PID_COS || { echo "COS download failed!"; exit 1; }
          }

          cleanup_disk &
          CLEANUP_PID=$!
          download_otas

          wait $CLEANUP_PID
          ls -lh OTA_zip/
          df -h

      - name: Install dependencies
        run: |
          sudo apt update
          sudo apt install -y \
            jq aria2 erofs-utils erofsfuse \
            brotli lz4 xz-utils unzip zip

          echo "Installing payload-dumper-go..."
          PD_URL="https://github.com/ssut/payload-dumper-go/releases/download/1.3.0/payload-dumper-go_1.3.0_linux_amd64.tar.gz"
          curl -sL "$PD_URL" -o dumper.tar.gz
          tar -xzf dumper.tar.gz
          sudo mv payload-dumper-go /usr/local/bin/payload-dumper-go
          rm dumper.tar.gz

      - name: Setup extraction paths
        run: |
          WORK_DIR=$(mktemp -d -t odmwork.XXXXXX)

          echo "WORK_DIR=${WORK_DIR}" >> "$GITHUB_ENV"
          echo "MOUNT_POINT=${WORK_DIR}/erofs_mount" >> "$GITHUB_ENV"

      - name: Extract ODM images
        run: |
          source .github/scripts/extract_odm_lib.sh

          WORK_DIR="${{ env.WORK_DIR }}"

          extract_and_clean "OTA_zip/oos.zip" "odm_oos.img" "$WORK_DIR"
          extract_and_clean "OTA_zip/cos.zip" "odm_cos.img" "$WORK_DIR"

          echo "odm_img_path=$WORK_DIR/odm_cos.img" >> "$GITHUB_ENV"

      - name: Update ODM files
        run: |
          source .github/scripts/extract_odm_lib.sh

          MOUNT_POINT="${{ env.MOUNT_POINT }}"
          REPO_ODM_ROOT="module/odm"
          HISTORY_FILE=".github/logs/odm_changes.md"
          CURRENT_REPORT="current_report.txt"

          mount_odm "${{ env.odm_img_path }}" "$MOUNT_POINT"

          {
            echo "## Update: $RELEASE_TAG"
            echo "- **Date:** $(date)"
            echo "- **Source COS:** ${{ inputs.cos_version }}"
            echo "- **Changes:**"
            echo '```diff'
          } > "$CURRENT_REPORT"

          find "$REPO_ODM_ROOT" -type f | sort | while read -r LOCAL_FILE; do
            REL_PATH="${LOCAL_FILE#"$REPO_ODM_ROOT"/}"
            SOURCE_FILE="$MOUNT_POINT/$REL_PATH"

            if [ -e "$SOURCE_FILE" ] || [ -L "$SOURCE_FILE" ]; then
              if ! cmp -s "$LOCAL_FILE" "$SOURCE_FILE" 2>/dev/null; then
                cp -Pf "$SOURCE_FILE" "$LOCAL_FILE"
                echo "! [UPDATED]   $REL_PATH" >> "$CURRENT_REPORT"
              fi
            else
              echo "- [MISSING]   $REL_PATH" >> "$CURRENT_REPORT"
            fi
          done

          {
            echo '```'
            echo
          } >> "$CURRENT_REPORT"

          touch "$HISTORY_FILE"
          cat "$CURRENT_REPORT" "$HISTORY_FILE" > temp && mv temp "$HISTORY_FILE"

          cat "$CURRENT_REPORT"

          unmount_odm "$MOUNT_POINT"

      - name: Cleanup temporary files
        run: rm -rf "${{ env.WORK_DIR }}" OTA_zip

      - name: Commit ODM changes
        run: |
          git config --global user.name 'github-actions[bot]'
          git config --global user.email 'github-actions[bot]@users.noreply.github.com'
          git add module/odm .github/logs/odm_changes.md
          git commit -m "feat: update ODM for $RELEASE_TAG"
          git push

  release:
    needs: [check-tag, update-odm]
    uses: ./.github/workflows/release.yml
    with:
      version: ${{ needs.check-tag.outputs.release_tag }}
      changelog_notes: "* Update ODM blobs from COS: ${{ inputs.cos_version }}"
      release_notes_footer: |
        ---
        ### Build Info
        - **Target Version:** `${{ needs.check-tag.outputs.release_tag }}`
        - **Source COS Version:** `${{ inputs.cos_version }}`

  record-history:
    needs: [check-tag, release]
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          ref: ${{ github.ref_name }}

      - name: Install yq
        run: |
          sudo wget -qO /usr/local/bin/yq https://github.com/mikefarah/yq/releases/latest/download/yq_linux_amd64
          sudo chmod +x /usr/local/bin/yq

      - name: Update OTA history
        env:
          TAG_VER: ${{ needs.check-tag.outputs.release_tag }}
          COS_VER: ${{ inputs.cos_version }}
        run: |
          [ -f .github/logs/ota_history.yml ] || echo "[]" > .github/logs/ota_history.yml

          TS=$(date -u --iso-8601=seconds)
          export TS

          yq -i '. += [{
            "oos_version": env(TAG_VER),
            "cos_version": env(COS_VER),
            "processed_at": env(TS)
          }]' .github/logs/ota_history.yml

          git config --global user.name 'github-actions[bot]'
          git config --global user.email 'github-actions[bot]@users.noreply.github.com'
          git add .github/logs/ota_history.yml
          git commit -m "chore: update OTA history for $TAG_VER [skip ci]"
          git push
